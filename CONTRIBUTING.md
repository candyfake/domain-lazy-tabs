# 参与贡献

欢迎提交兼容性问题、文档改进和范围明确的代码修改。请保持项目专注于「按目标域名延迟链接导航」。

## 报告问题

请在 GitHub Issues 中提供：

- 操作系统、Chrome 版本和扩展版本。
- 来源页面与目标链接的域名；非公开页面请用最小复现页面代替。
- 配置的域名列表，以及具体打开方式：右键菜单、Ctrl/Cmd + 左键或中键。
- 期望结果、实际结果、能否稳定复现。
- 是否同时启用了其他链接处理、标签页管理或油猴脚本。

截图、地址和日志可能含有个人信息。请删除用户名、私密帖子、查询参数中的凭据或其他敏感内容。

## 本地开发

将仓库中的 `extension/` 作为已解压扩展加载。修改文件后，在 `chrome://extensions` 重新加载扩展，并刷新测试来源页面。

修改链接处理代码时，请验证这些实际行为：

1. 正常左键访问不受影响。
2. 白名单域名与子域名匹配正确，相似域名不会误匹配。
3. 原生右键新开、Ctrl/Cmd + 左键、中键打开时，后台先显示本地等待页面。
4. 激活等待标签页后只导航一次，并保留原始路径、查询参数和片段。
5. Alt + 右键可以复制原始地址；关闭总开关后恢复原始行为。
6. 域名配置更新、快速切换标签页及多个待打开链接不会产生重复导航。
7. 页面移除或动态替换链接、扩展重载等场景能合理恢复或提供清晰回退。

验证网络边界时，应观察目标导航请求，而不是仅凭空白画面判断「没有加载」。来源页面的预取请求需要单独区分。请明确说明哪些结果来自自动测试、哪些来自真实浏览器操作，不能把模拟测试视作原生右键菜单已验证。

## 提交代码

- 每个 PR 聚焦一个问题，并描述复现方法、解决方式与验证结果。
- 不引入遥测、服务器依赖、远程执行代码或不必要权限。
- 代码、默认配置与隐私说明保持一致。
- 引用第三方代码或素材时，说明来源与许可证。
- 提交前检查差异，不提交私密数据、浏览器配置、令牌或个人文件。

贡献按本项目的 MIT 许可证提供。
