# Changelog

## 1.1.0 — 2026-09-24

- 新增可选提前加载模式：默认 3 个自动名额，可设 1～10 个，受管域名与浏览器窗口共用名额。
- 自动加载的帖子关闭或离开对应域名后，按创建顺序补上下一个。加载完成、切换查看均不释放名额。
- 手动点击等待标签立即打开，不挤占自动名额、不休眠已打开网页。保留默认按需模式。
- 使用事件驱动的后台处理程序与本机会话存储，后台休眠后恢复队列；增加 `tabs` 权限和 Chrome 102 最低版本要求。
- 去掉等待标签标题中的「待查看」前缀，保留链接标题，并使用冰棱雪花图标标识等待状态。
- 同步更新工具栏图标及 16、32、48、128 像素图标文件；原图与生成提示词随源码提供。

## 1.0.0 — 2026-09-24

- 新增基于本地等待页面的链接延迟导航：后台打开时先保留占位页，切到前台后才导航至目标。
- 支持目标域名白名单，默认 `linux.do` 及子域名；支持配置列表和总开关。
- 支持原生右键菜单、Ctrl/Cmd + 左键和中键打开链接。
- 支持 Alt + 右键临时保留原始链接。
- 仅本地处理网址与设置，无服务器、分析统计或远程执行代码。
- 提供 Manifest V3 扩展、中文使用说明、隐私说明及 MIT 许可证。
