{
  "name": "domain-lazy-tabs",
  "version": "1.1.0",
  "private": true,
  "description": "Open links to selected domains as local parked tabs until activation.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser.cjs && node tests/browser-queue.cjs",
    "package": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package.ps1"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
