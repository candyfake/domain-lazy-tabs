# 隐私说明

适用项目：Domain Lazy Tabs

生效日期：2026 年 9 月 24 日

Domain Lazy Tabs 在浏览器本地处理链接和配置。扩展没有开发者服务器，不包含遥测、分析 SDK、广告或远程托管的执行代码。

## 本地处理的信息

- **域名列表与启用状态**：存储在浏览器的扩展本地存储中，用于决定哪些链接需要延迟导航。
- **用户操作的链接地址**：页面脚本检查链接目标，以匹配域名。对于被延迟打开的链接，原始 URL 保留在相应扩展等待页面的 URL 片段中，供你切换到标签页后导航使用。
- **用户操作的链接文字或标题**：读取该链接的 `textContent`，为空时读取其 `title`，用于为等待标签页提供可辨认的标题。保存的文字截断至前 200 个字符，与目标 URL 一起放在本地等待页面的 URL 片段中；不会读取或保存整页正文。
- **页面可见状态和链接打开操作**：用于区分后台等待与前台访问。

本扩展不建立浏览历史数据库，不读取表单内容、账号密码或 Cookie，也不向开发者及第三方发送以上信息。它不请求 Chrome 的 `tabs` 或 `history` 权限。

## 为什么需要在普通网页上运行

你可能从任意来源网页打开指向指定域名的链接。因此，扩展的页面脚本在 HTTP/HTTPS 网页中运行，处理标准链接和相关点击、右键操作。Chrome 可能为此显示访问网站数据的权限提示。域名列表限定的是**待打开链接的目标域名**，而不是脚本运行的来源域名。

## 数据边界

- 本地等待页面的 URL 包含原始目标地址及截断后的链接文字，可能包含来源链接中原本存在的敏感信息，不应被当作隐藏浏览目的的工具。浏览器自身及拥有相应权限的软件仍可能访问这些本地信息。不要公开分享包含私密目标的等待页面 URL。
- 切换到等待标签页后，浏览器会导航到目标网站；此后的网络连接与网站数据处理遵循该网站及浏览器的规则。
- 扩展不阻止来源页面的预取、既有网络请求或其他扩展的行为。
- 将问题提交至 GitHub 是你主动向 GitHub 公开或提供的信息，不属于扩展的自动数据收集。提交截图和日志前请移除个人信息、私密 URL 和令牌。

## 删除与控制

可在扩展设置中修改域名或关闭处理，关闭等待标签页可移除该标签页中的待访问地址及标题。卸载扩展会移除其本地配置；浏览器自行保存的历史、备份或其他数据需要通过浏览器设置管理。

本版本不支持无痕窗口。功能或数据处理方式如有变化，本文件将随版本更新。一般隐私问题可通过本项目的 GitHub Issues 反馈；请勿在公开 Issue 中提交敏感信息。安全漏洞请参阅 [SECURITY.md](SECURITY.md)。
